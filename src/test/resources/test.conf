emdr.url = ""
activemq.url = ""

emdr.input = "direct:emdr.input"
emdr.json  = "direct:emdr.json"
history.output = "mock:history"
market-report.output = "mock:market-report"
error.output = "mock:errors"

order-filter {
  stations: [1] 
}
